*.iml
.gradle/
.kotlin/
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Серверные секреты — НЕ КОММИТИТЬ
server/api/config.php

# Playwright MCP артефакты (снимки/трейсы), которые создаются при верификации UI
/.playwright-mcp/
/whereami-*.png

# Пользовательские скриншоты/фото с телефонов — могут содержать чувствительные данные (IMEI и т.п.)
/*.jpg
/*.jpeg
/screen*.png

# Локальные секреты (например, GITHUB_TOKEN для скриптов) — НЕ КОММИТИТЬ
.env
.env.local

# Release keystore — НЕ КОММИТИТЬ (генерится один раз, base64 хранится в GitHub Secrets)
*.jks
*.keystore
keystore.b64.txt

